{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "mysecretpassword",
  "database": "postgres",
  "synchronize": true,
  "logging": false,
  "entities": [
     
  ],
  "migrations": [
     "src/migration/**/*.ts",
     "dist/modules/*.entity{.ts,.js}"
  ],
  "subscribers": [
     "src/subscriber/**/*.ts"
  ],
  "cli": {

     "migrationsDir": "db/migration"
    
  }
}